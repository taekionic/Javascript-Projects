function multiply() {   //defines the function named "multiply"
    var solution = 5*6; //devines a variable named solution, which is equal to the product of 5 and 6
    solution += " is the solution!" //concatenates the string "is the solution" to the variable solution.
    document.getElementById('click'); //calls the  element with the id 'click'. Here, this is <button>
    document.write(solution); //prints the solution variable on function execution.
    
}
    

